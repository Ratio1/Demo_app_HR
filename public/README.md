# The application serves no public files today: every icon is an inlined SVG component and
# Tailwind's stylesheet is emitted into .next/static. The directory exists because the
# Dockerfile copies it into the image and Next.js serves it as the site root.
